{
  "installCommand": "npm install -g pnpm@8 --silent && cd ../.. && pnpm install --no-frozen-lockfile",
  "buildCommand": "npx prisma@5.22.0 generate",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.ts"
    }
  ]
}
