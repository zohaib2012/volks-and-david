{
  "installCommand": "npm install --legacy-peer-deps --ignore-scripts --include=dev",
  "buildCommand": "node_modules/.bin/prisma generate --schema=prisma/schema.prisma",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.ts"
    }
  ]
}
